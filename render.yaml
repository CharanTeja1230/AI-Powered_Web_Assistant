services:
  - type: web
    name: lumo-ai-assistant
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn etc.tool.openapi:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.11
