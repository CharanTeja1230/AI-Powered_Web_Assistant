[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "lumo-oracle-platform"
version = "1.0.0"
description = "LUMO - AI Chat Assistant by Charan"
readme = "README.md"
authors = [{ name = "Charan", email = "charan.teja@lumo.ai" }]
urls = { Homepage = "https://lumo-ai-assistant.onrender.com" }
requires-python = ">=3.9"
dependencies = [
    "fastapi",
    "uvicorn",
    "g4f",
    "requests",
    "aiohttp",
    "jinja2"
]

[tool.fastapi]
entrypoint = "main:app"
